/**
 * Imported projects: SBOM-sourced dependency lists stored in embedded H2 — deliberately
 * OUTSIDE the knowledge graph — and matched on demand against graph vulnerabilities
 * through {@link dev.argusgraph.graph.GraphAPI} only.
 */
package dev.argusgraph.project;
